// Lexer section

proto_separator "::"
identifier "[A-Z][a-z]_*[0-9]"
number "[0-9]"
dot "."
separator ","

<--->

// AST section

expression:
  // First "|" then token <what can come after>
  | write_cmd <identifier number>
  {
  }
  | read_cmd <identifier number>
  {
  }